<Window x:Class="WpfApp1.Views.TaskEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Task Editor"
        Width="460" Height="420"
        MinWidth="400" MinHeight="340"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        ShowInTaskbar="False"
        WindowStyle="ToolWindow"
        Background="{DynamicResource WindowBg}">

    <Window.Resources>
        <SolidColorBrush x:Key="WindowBg" Color="#F9FAFB"/>
        <SolidColorBrush x:Key="BgCard" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="ItemBg" Color="#F9FAFB"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#1F2937"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#6B7280"/>
        <SolidColorBrush x:Key="BorderColor" Color="#E5E7EB"/>
        <SolidColorBrush x:Key="BtnSecondaryBg" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="BtnSecondaryFg" Color="#1F2937"/>
        <SolidColorBrush x:Key="PrimaryColor" Color="#6366F1"/>

        <Style TargetType="Button" x:Key="SecondaryBtn">
            <Setter Property="Background" Value="{DynamicResource BtnSecondaryBg}"/>
            <Setter Property="Foreground" Value="{DynamicResource BtnSecondaryFg}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="PrimaryBtn">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="18,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" x:Name="EditorTitle" Text="Task Editor"
                   FontSize="18" FontWeight="Bold"
                   Foreground="{DynamicResource TextPrimary}"
                   Margin="0,0,0,16"/>

        <!-- Task Name -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" x:Name="NameLabel" Text="Task Name"
                       FontSize="12" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextSecondary}"
                       Margin="0,0,0,4"/>
            <TextBox Grid.Row="1" x:Name="TaskNameBox"
                     FontSize="14" Padding="10,8"
                     VerticalContentAlignment="Center"/>
        </Grid>

        <!-- Task Type -->
        <Grid Grid.Row="2" Margin="0,0,0,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" x:Name="TypeLabel" Text="Task Type"
                       FontSize="12" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextSecondary}"
                       Margin="0,0,0,4"/>
            <StackPanel Grid.Row="1" Orientation="Horizontal">
                <Button x:Name="ShortTermBtn" Content="Short" Tag="ShortTerm"
                        Style="{StaticResource SecondaryBtn}"
                        Click="TypeBtn_Click"
                        FontSize="12" Padding="14,7" Margin="0,0,6,0"/>
                <Button x:Name="DailyBtn" Content="🔄 Daily" Tag="Daily"
                        Style="{StaticResource SecondaryBtn}"
                        Click="TypeBtn_Click"
                        FontSize="12" Padding="14,7" Margin="0,0,6,0"/>
                <Button x:Name="LongTermBtn" Content="🎯 Long" Tag="LongTerm"
                        Style="{StaticResource SecondaryBtn}"
                        Click="TypeBtn_Click"
                        FontSize="12" Padding="14,7"/>
            </StackPanel>
        </Grid>

        <!-- Ability Points -->
        <Grid Grid.Row="3" Margin="0,0,0,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" x:Name="PointsLabel" Text="Points awarded on completion"
                       FontSize="12" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextSecondary}"
                       Margin="0,0,0,4"/>
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="AbilityPointsList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="8,5" Margin="0,0,0,3"
                                    Background="{DynamicResource ItemBg}" CornerRadius="4"
                                    BorderBrush="{DynamicResource BorderColor}" BorderThickness="0.5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Icon}" FontSize="13"
                                                   VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <TextBlock Text="{Binding Name}" FontSize="13"
                                                   Foreground="{DynamicResource TextPrimary}"
                                                   VerticalAlignment="Center"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                        <Button Content="−" Style="{StaticResource SecondaryBtn}"
                                                FontSize="10" Padding="6,2"
                                                Click="MinusBtn_Click"
                                                Tag="{Binding}"/>
                                        <TextBlock Text="{Binding Points}" FontSize="14" FontWeight="SemiBold"
                                                   Width="24" TextAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   Foreground="{StaticResource PrimaryColor}"
                                                   Margin="4,0"/>
                                        <Button Content="+" Style="{StaticResource SecondaryBtn}"
                                                FontSize="10" Padding="6,2"
                                                Click="PlusBtn_Click"
                                                Tag="{Binding}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CancelBtn" Content="Cancel"
                    Style="{StaticResource SecondaryBtn}"
                    Click="CancelBtn_Click"
                    FontSize="13" Padding="16,8" Margin="0,0,8,0"/>
            <Button x:Name="SaveBtn" Content="Save"
                    Style="{StaticResource PrimaryBtn}"
                    Click="SaveBtn_Click"
                    FontSize="13" Padding="20,8"/>
        </StackPanel>
    </Grid>
</Window>
